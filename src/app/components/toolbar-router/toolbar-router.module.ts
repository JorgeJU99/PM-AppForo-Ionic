import { NgModule } from '@angular/core';
import { IonicModule } from '@ionic/angular';
import { ToolbarRouterComponent } from './toolbar-router.component';

@NgModule({
	imports: [IonicModule],
	declarations: [ToolbarRouterComponent],
	exports: [ToolbarRouterComponent],
})
export class ToolbarRouterModule {}
